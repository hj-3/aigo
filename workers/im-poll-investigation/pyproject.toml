[project]
name = "worker-im-poll-investigation"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.35.0",
    "structlog>=24.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
